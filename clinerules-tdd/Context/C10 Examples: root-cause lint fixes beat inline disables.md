---
tags: [context, example]
node_type: CONTEXT
context_type: example
grounds_count: 4
source: "adamglang/sovereign-core .clinerules"
---
# 🗒 Examples: root-cause lint fixes beat inline disables

> ❌ // eslint-disable-next-line ... · ✅ move test utilities to a separate config · ✅ replace export * with named exports · ✅ capture ref values in effect scope to satisfy exhaustive-deps.

**Node type:** CONTEXT — no deontic force, cannot be violated; participates only in support-flavored relations (defines / exemplifies / motivates), and is one-to-many by nature.

## Role
Hand-tagged PREFERENCE in the CSV, but it prescribes nothing — it illustrates the escalation ladder and the pragma ban with concrete cases. Examples-misread-as-preferences was a recurring annotation issue (see also C11).

## Grounds (4 rules)
- **support (exemplifies)** → [[clinerules-tdd/Rules/R26 NEVER disable linting, compilation, or type errors|R26 NEVER disable linting, compilation, or type errors]]
- **support (exemplifies)** → [[clinerules-tdd/Rules/R27 No suppression pragmas|R27 No suppression pragmas]]
- **support (exemplifies)** → [[clinerules-tdd/Rules/R28 Fix the root cause (escalation ladder)|R28 Fix the root cause (escalation ladder)]]
- **support (exemplifies)** → [[clinerules-tdd/Rules/R29 Exception for generated and third-party code|R29 Exception for generated and third-party code]]
